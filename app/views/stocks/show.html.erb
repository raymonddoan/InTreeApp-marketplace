<p id="notice"><%= notice %></p>

<div class="bg-primary h-100 min-h-screen">
  <h1 class="text-2xl text-center py-6 text-white"><%= @stock_orders[0].stock.name %> Sell Orders</h1>

  <div class="flex justify-center w-full">
    <div class="rounded-lg border bg-white shadow m-5 md:w-1/2 w-96">
      <%# Loop to show all the available orders %>
      <% @stock_orders.each do |stock_order| %>
      <div class="p-2">
        <div class="p-3 rounded-lg border bg-gray-200 shadow flex flex-row justify-between">
          <div class="flex flex-column">
            <ul>
              <li class="text-lg"><%= stock_order.stock.name %></li>
              <li><%= number_to_currency(stock_order.price) %> (Quantity: <%= stock_order.quantity %>)</li>
              <li>Expires: <%= stock_order.expiry_date %></li>
              <% stock_order.filled %>
            </ul>
          </div>
          <div class="w-10">
            <%= link_to image_tag("show-dot.png"), stock_order %>
          </div>
        </div>
      </div>
      <% end %>

      <%# Place Order %>
      <% if user_signed_in? %>
      <div class="p-1">
        <div class="p-3 rounded-lg border bg-secondary shadow grid justify-item-stretch">
          <p class="text-lg text-white text-center">+ <%= link_to 'Place Order', new_sell_order_path, class: "justify-self-auto" %></p>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>